# 自定义模型目录

此目录用于存放用户自定义的 ONNX 嵌入模型。

## 支持的模型

支持所有 Transformers.js 兼容的 ONNX 嵌入模型，包括：
- BGE 系列（如 bge-small-zh-v1.5）
- E5 系列（如 multilingual-e5-small）
- MiniLM 系列（如 all-MiniLM-L6-v2）
- GTE 系列（如 gte-small）

## 目录结构

```
public/models/custom/Xenova/
├── 模型名称/
│   ├── config.json
│   ├── tokenizer.json
│   ├── tokenizer_config.json
│   └── onnx/
│       └── model_quantized.onnx
```

## 安装步骤

1. 从 Hugging Face 的 Xenova 转换版页面下载模型文件
2. 创建以模型名命名的文件夹
3. 将下载的 4 个文件放入文件夹
4. 重启 dev server
5. 设置页点击"扫描"按钮
6. 发现后点击选用

## 示例

```
public/models/custom/Xenova/all-MiniLM-L6-v2/
├── config.json
├── tokenizer.json
├── tokenizer_config.json
└── onnx/
    └── model_quantized.onnx
```

## 注意事项

- 模型文件较大（23-120 MB），不适合上传到 GitHub
- 每个模型需要约 100-500 MB 内存加载
- 首次使用时需要下载模型文件到浏览器缓存
